<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RmrNal1.NewPage2"
             Title="Vizitka">

    <VerticalStackLayout
        VerticalOptions="CenterAndExpand"
        HorizontalOptions="CenterAndExpand"
        >

        <Label Text="Razvoj mobilnih rešitev"
               FontSize="Medium"
               TextColor="#006385"
               HorizontalOptions="Center"
               VerticalOptions="Center" />
        <Label Text="ITK VS"
       FontSize="Medium"
       TextColor="#006385"
       HorizontalOptions="Center"
       VerticalOptions="Center" />
        <Label Text="1.1"
       FontSize="Medium"
       TextColor="#006385"
       HorizontalOptions="Center"
       VerticalOptions="Center" />
        <Label Text="2024 © Tjan Kazar"
       FontSize="Medium"
       TextColor="#006385"
       HorizontalOptions="Center"
       VerticalOptions="Center" />

    </VerticalStackLayout>
</ContentPage>
